import { LinearProgress,Box } from "@mui/material"

const LinearProgressLine = () => {
    return <Box sx={{width:"100vw"}}>
       <LinearProgress color="secondary"/>
    </Box>
}

export default LinearProgressLine